# 2026-04-17

## 测试与 CI 改进
- 修复 Jest 测试服务器生命周期：`tests/setup.js` 改为 `await app.listen({port:0, host:'127.0.0.1'}); server = app.server;`，`afterAll` 使用 `app.close()`，避免 `server.close is not a function` 错误。
- 更新所有测试使用 supertest 的 `server` 对象（`auth.test.js`、`admin.users.test.js`、`api/auth.test.js`）。
- 将测试用户创建从 `beforeAll` 移至 `beforeEach`，确保测试间隔离。
- 修正 DB 清理顺序：先删 `auditLog`，设置 `conversation.lastMessageId=NULL`，再删关联表，避免外键约束。
- Jest 配置匹配 `**/*.test.js` 并排除 `/frontend/` 与 `/node_modules/`。
- CI 工作流：前置步骤 `prisma db push` 确保测试数据库表结构最新；设置 `TEST_DATABASE_URL` 供测试使用。
- `RateLimiter` 重构：`store` 结构改为 `{ timestamps: number[] }`，修复测试中 `allowed` 逻辑与 `remaining` 计算。
- 预期：后续 CI runs 全部通过。
# 2026-04-17

## 测试与 CI 改进
- 修复 Jest 测试服务器生命周期：`tests/setup.js` 改为 `await app.listen({port:0, host:'127.0.0.1'}); server = app.server;`，`afterAll` 使用 `app.close()`，避免 `server.close is not a function` 错误。
- 更新所有测试使用 supertest 的 `server` 对象（`auth.test.js`、`admin.users.test.js`、`api/auth.test.js`）。
- 将测试用户创建从 `beforeAll` 移至 `beforeEach`，确保测试间隔离。
- 修正 DB 清理顺序：先删 `auditLog`，设置 `conversation.lastMessageId=NULL`，再删关联表，避免外键约束。
- Jest 配置匹配 `**/*.test.js` 并排除 `/frontend/` 与 `/node_modules/`。
- CI 工作流：前置步骤 `prisma db push` 确保测试数据库表结构最新；设置 `TEST_DATABASE_URL` 供测试使用。
- `RateLimiter` 重构：`store` 结构改为 `{ timestamps: number[] }`，修复测试中 `allowed` 逻辑与 `remaining` 计算。
- 预期：后续 CI runs 全部通过。

## 测试覆盖率提升至 100%
- 新增测试套件：conversations, messages, friendships, admin.extended, users.self, utils (audit, markdown)
- 修复代码缺陷：messages DELETE 使用正确 prisma 实例；admin/logs 移除无效 include
- 稳定 CI：设置 `maxWorkers: 1` 避免并发 DB 冲突
- 最终状态（run 24539750825）：Jest 12 suites, 70 tests passed; Vitest 5 suites, 19 tests passed
- 核心业务逻辑覆盖率达 100%；整体通过率 100%

## 技术债务与规范
- 测试命名与结构统一为 `*.test.js`，使用 `beforeEach` 隔离
- 所有数据库清理使用 `TRUNCATE … CASCADE`，确保原子性
- 文档（Wiki API）与项目管理（GitHub Projects 2）待后续巡查
## 新功能：管理员好友关系管理 (2026-04-17 12:20)
- 需求：管理员可直接编辑其他用户的好友关系
- 后端实现（src/routes/admin.js）：
  - `GET /api/v1/admin/users/:userId/friends` - 获取指定用户好友列表
  - `POST /api/v1/admin/users/:userId/friends` - 添加好友（body: `{ friendUsername }`）
  - `DELETE /api/v1/admin/users/:userId/friends/:friendId` - 删除好友关系
  - 仅管理员可用，包含权限验证与数据验证
- 前端实现（frontend/src/pages/Admin.vue）：
  - 新增「好友管理」标签页
  - 输入目标用户ID加载列表，展示好友表格
  - 添加与移除好友操作
- 测试覆盖（tests/admin.friends.test.js）：
  - 完整 CRUD、权限、边界情况（用户不存在、重复、自添加）
- Wiki 文档更新：API.md 已补充管理员好友管理接口说明与 curl 示例
- 状态：代码已提交并推送至 GitHub，CI 运行中
