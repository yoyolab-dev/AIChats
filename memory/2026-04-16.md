# 2026-04-16

## 移动端适配完成
- 前端布局全面响应式：桌面端侧边栏，移动端顶部导航+抽屉菜单
- 断点 768px，导航菜单在大屏侧边、小屏抽屉
- 登录页独立居中，卡片宽度自适应
- 所有页面按钮统一 large 尺寸，输入框全宽，确保点击区域 ≥44px
- Admin 表格支持横向滚动

## 后端与路由修复
- 问题：后端容器日志反复出现 `SyntaxError: Named export 'Router' not found` 和 `does not provide an export named 'default'`。
- 修复：
  - 将 `src/routes/*.js` 中 `Router()` 模式重构为 Fastify 标准插件函数：
    ```js
    export default async function (fastify, opts) {
      fastify.addHook('preHandler', authenticate)
      fastify.get('/path', handler)
      // ...
    }
    ```
  - 删除 `import { Router } from 'fastify'`
  - 修正 `auth.js` 的导入为 `import { authenticate as authPlugin } from '../plugins/auth.js'`
- 其他修复：
  - `admin.js` 移除 `done` 回调 (commit bc28614)
  - `server.js` 设置 `host: '0.0.0.0'` 确保 Traefik 可达 (commit b1c85de)
- CI 改进：因老版本 Podman 不支持 `kube play --pull`，改为先执行 `podman pull` 再 `kube play --replace` (commit cfc9f52)。
- 部署后验证：
  - `https://api.oujun.work/health` 返回 200 OK
  - 前端 `https://pm.oujun.work` 完全可用，无 JS 报错

## 认证与登录实现
- 新增 `POST /api/v1/auth/login`：验证 API Key 并返回用户信息 (commit 33ddc3a)
- 修复 `request.httpErrors` undefined 问题：改为使用 `reply.code(401).send()` (commit fde6a34)
- 前端创建 `.env.production` 并设置 `VITE_API_BASE_URL=https://api.oujun.work` 解决生产环境请求错误 (commit 07dfd93)
- 生成管理员 API Key `sk-admin-fh4hyx8a2gc` 用于测试

## 新增功能实现 (2026-04-16 22:00)
- 需求：
  - 登录后隐藏登录功能
  - 右上角添加退出登录按钮
  - 管理员用户管理界面（增删改）
- 实现：
  - 前端：`App.vue` 加入 `useAuthStore`，动态菜单，logout 按钮
  - 前端：`Admin.vue` 新增用户管理弹窗、CRUD 逻辑
  - 后端：`admin.js` 扩展 `/api/v1/admin/users` 路由（列表、创建、更新、软删除）
  - 引入 `n-dialog-provider` 以支持 `useDialog()` (commit 37ff551)
- 相关 Issues: #23, #24, #25, #26
- 提交记录:
  - 7b3ba6d feat: implement login hiding, logout button, and admin user management UI + backend CRUD
  - 2f74358 fix(admin): correct v-model binding for displayName in user modal
  - e7b1c00 feat(admin): remove password requirement for user creation, add displayName support
  - 37ff551 fix(ui): add n-dialog-provider to App.vue to support useDialog()
- 部署：CI 自动构建、推送、部署完成
- 验证：管理员登录后可访问用户管理，创建新用户将生成 API Key。

## 测试与 CI 改进 (持续)
- 后端测试：新增 `tests/auth.test.js` (登录/认证), `tests/admin.users.test.js` (管理员用户 CRUD)
- 前端测试：新增 `frontend/tests/unit/auth.store.test.js` (Pinia auth store unit tests)
- 测试配置迭代：
  - Jest: `testMatch: ['**/*.test.js']` + `testPathIgnorePatterns: ['/frontend/']` 避免与 Vitest 冲突
  - Vitest: 配置 `vite.config.js` 使用 jsdom 环境
- 测试架构：
  - 使用 supertest 直接对 Fastify 实例 `app` 进行测试，由 supertest 自动管理服务生命周期（启动/关闭），避免手动 `listen`/`close` 导致的地址/端口错误
  - `tests/setup.js` 负责 Prisma 连接和 `beforeEach` 数据清理
  - CI 工作流添加 PostgreSQL 15 服务
  - 使用 `npx prisma db push --accept-data-loss` 同步测试库 Schema
  - 后端测试环境设置 `DATABASE_URL` 和 `TEST_DATABASE_URL`
- CI 流程：`npm ci` → 后端测试 → 前端测试 → 构建推送镜像 → 部署
- 关键修复：
  - `tests/api/auth.test.js` 从使用 `server` 改为 `app`
  - `tests/auth.test.js` 和 `tests/admin.users.test.js` 统一使用 `app`
  - 移除 `setup.js` 中的手动 `listen/close`，改用 supertest 自动管理
- 提交：
  - f79f8f9 test: use supertest with app instance, remove manual server management
  - 34719a3 test: exclude frontend dir from Jest, ensure only backend tests run
  - ae56df9 test: fix jest testMatch, use db push for CI, start server in tests
  - 618a70c test: fix jest testMatch, use db push for CI, start server in tests (previous)

## 文档更新
- Wiki API 文档完全重写，列出所有端点及其参数、响应示例、cURL 命令
- 已推送至 GitHub Wiki (`yoyolab-dev/AIChats.wiki`)

## 项目与文档
- GitHub Projects 阶段卡片全部完成（23/23 包括新增）
- Issues: 创建并随后关闭 #23, #24, #25, #26
- Actions: 最近 runs 在修复后全部成功

## 提交记录 (主要)
- 847296e feat(mobile): 实现移动端自适应布局和触摸优化
- dbcfc5e fix(backend): correct authPlugin import naming
- cfc9f52 ci: use explicit podman pull for deployment
- 33ddc3a feat(backend): add auth login route
- debbe83 refactor(prisma): use onRequest to decorate request.prisma
- 1985270 refactor(routes): use fastify.prisma directly
- e29c7f3 test(debug): add _debug/prisma route
- 6da92db chore(deps): downgrade @fastify/helmet
- bc28614 fix(admin): remove done callback
- b1c85de fix(server): listen on 0.0.0.0
- 07dfd93 chore(env): add frontend .env.production
- fde6a34 fix(auth): use reply.code(401) instead of request.httpErrors in preHandler
- 7b3ba6d feat: implement login hiding, logout button, and admin user management UI + backend CRUD
- 2f74358 fix(admin): correct v-model binding for displayName in user modal
- e7b1c00 feat(admin): remove password requirement for user creation, add displayName support
- 37ff551 fix(ui): add n-dialog-provider to App.vue to support useDialog()
- 618a70c test: fix jest testMatch, use db push for CI, start server in tests
- 4bc9ece docs: update API reference with all endpoints including admin user management

## 最终状态
- 所有服务健康，CI 流水线应稳定通过测试并自动部署
- 前后端分离架构完善，API 文档齐全
- 新增管理员用户管理功能，可通过 API Key 认证的完整流程
- 测试覆盖后端核心认证、授权、管理员接口，前端覆盖 auth store 单元
