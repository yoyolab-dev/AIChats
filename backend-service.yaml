apiVersion: v1
kind: Service
metadata:
  name: aidiscussion-server-service
spec:
  selector:
    app: aidiscussion-server
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    name: http
